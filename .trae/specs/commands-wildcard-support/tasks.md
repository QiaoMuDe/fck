# Tasks

- [x] Task 1: 为 7 个命令的 CLI 层添加通配符展开（可并行修改）
  - [x] `head.go` — 导入 `fs`，调用 `fs.ExpandFiles(args)`，结果赋给 `Targets`
  - [x] `hash.go` — 导入 `fs`，调用 `fs.ExpandFiles(args)`，结果赋给 `TargetPaths`
  - [x] `size.go` — 导入 `fs`，调用 `fs.ExpandFiles(args)`，结果赋给 `Args`
  - [x] `touch.go` — 导入 `fs`，调用 `fs.ExpandFiles(args)`，结果赋给 `Targets`
  - [x] `truncate.go` — 导入 `fs`，调用 `fs.ExpandFiles(args)`，结果赋给 `Targets`
  - [x] `iconv.go` — 导入 `fs`，调用 `fs.ExpandFiles(args)`，结果赋给 `Files`
  - [x] `json.go` — 导入 `fs`，调用 `fs.ExpandFiles(args)`，结果赋给 `Files`

# Task Dependencies

无依赖，所有 7 个文件可并行修改。
