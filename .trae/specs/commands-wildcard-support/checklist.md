# Checklist

- [x] `head.go` 调用 `fs.ExpandFiles` 展开通配符
- [x] `hash.go` 调用 `fs.ExpandFiles` 展开通配符
- [x] `size.go` 调用 `fs.ExpandFiles` 展开通配符
- [x] `touch.go` 调用 `fs.ExpandFiles` 展开通配符
- [x] `truncate.go` 调用 `fs.ExpandFiles` 展开通配符
- [x] `iconv.go` 调用 `fs.ExpandFiles` 展开通配符
- [x] `json.go` 调用 `fs.ExpandFiles` 展开通配符
- [x] 编译通过
