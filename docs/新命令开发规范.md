# 新增命令开发规范

## 目录结构规范

所有命令必须遵循以下目录结构：

```
internal/
├── commands/
│   └── <command_name>/           # 命令目录（小写，如 mkdir, touch, rm）
│       └── cmd_<command_name>.go  # 业务逻辑文件（小写，如 cmd_mkdir.go）
└── cli/
    ├── <command_name>.go             # CLI 定义文件（小写，如 mkdir.go）
    └── root.go                    # 命令注册文件
```

## 命令命名规范

### 1. 目录和文件命名
- **命令目录**：小写，如 `mkdir`, `touch`, `rm`
- **业务逻辑文件**：`cmd_<command_name>.go`，如 `cmd_mkdir.go`
- **CLI 定义文件**：`<command_name>.go`，如 `mkdir.go`

### 2. 代码命名
- **包名**：与命令目录同名，小写，如 `package mkdir`
- **配置结构体**：`<Command>Config`，如 `MkdirConfig`
- **主函数**：`<Command>CmdMain`，如 `MkdirCmdMain`
- **命令变量**：`<Command>Cmd`，如 `MkdirCmd`
- **选项变量**：`<command><Option>`，如 `mkdirParents`, `mkdirMode`
- **运行函数**：`run<Command>`，如 `runMkdir`

## 业务逻辑文件规范

### 1. 文件结构模板

```go
package <command_name>

import (
	"fmt"
	"os"
	"path/filepath"
	// 其他必要的导入
)

// <Command>Config 配置结构体
type <Command>Config struct {
	Targets   []string
	Option1   bool
	Option2   int
	Option3   string
	Verbose   bool
}

// <Command>Stats 操作统计（可选）
type <Command>Stats struct {
	Processed int
	Errors    int
}

// <Command>CmdMain 主函数
func <Command>CmdMain(config <Command>Config) error {
	if len(config.Targets) == 0 {
		return fmt.Errorf("未指定要操作的目标")
	}

	stats := &<Command>Stats{}

	for _, target := range config.Targets {
		err := processTarget(target, config, stats)
		if err != nil {
			stats.Errors++
			return err
		}
	}

	if config.Verbose {
		fmt.Printf("操作完成: %d 个处理", stats.Processed)
		fmt.Println()
	}

	return nil
}

// processTarget 处理单个目标
func processTarget(path string, config <Command>Config, stats *<Command>Stats) error {
	// 处理逻辑
	return nil
}
```

### 2. 编码规范
- 使用函数级注释
- 错误处理使用 `fmt.Errorf` 包装错误
- 使用 `os.IsNotExist` 等标准错误判断
- 路径处理使用 `filepath.Abs` 获取绝对路径
- 时间处理使用 `time` 包

## CLI 定义文件规范

### 1. 文件结构模板

```go
package cli

import (
	"fmt"

	"gitee.com/MM-Q/fck/internal/commands/<command_name>"
	"gitee.com/MM-Q/qflag"
)

var <Command>Cmd *qflag.Cmd

var (
	<command>Option1 *qflag.BoolFlag   // 选项说明
	<command>Option2 *qflag.StringFlag // 选项说明
	<command>Verbose *qflag.BoolFlag   // 显示详细信息
)

func init() {
	<Command>Cmd = qflag.NewCmd("<command>", "<short>", qflag.ExitOnError)

	<command>Option1 = <Command>Cmd.Bool("option1", "o1", "选项说明", false)
	<command>Option2 = <Command>Cmd.String("option2", "o2", "选项说明", "default")
	<command>Verbose = <Command>Cmd.Bool("verbose", "v", "显示详细信息", false)

	cmdOpts := &qflag.CmdOpts{
		Desc: "命令描述",
		Notes: []string{
			"说明1",
			"说明2",
		},
		UseChinese: true,
	}

	if err := <Command>Cmd.ApplyOpts(cmdOpts); err != nil {
		panic(fmt.Errorf("apply opts err: %w", err))
	}

	<Command>Cmd.SetRun(run<Command>)
}

func run<Command>(cmd qflag.Command) error {
	config := <command_name>.<Command>Config{
		Targets:   cmd.Args(),
		Option1:  <command>Option1.Get(),
		Option2:  <command>Option2.Get(),
		Verbose:  <command>Verbose.Get(),
	}

	return <command_name>.<Command>CmdMain(config)
}
```

### 2. 选项定义规范
- **Bool 选项**：`Bool("long", "short", "说明", 默认值)`
- **String 选项**：`String("long", "short", "说明", 默认值)`
- **Int 选项**：`Int("long", "short", "说明", 默认值)`

### 3. 帮助文档规范
- `Desc`：简洁的命令描述
- `Notes`：详细的使用说明，每条说明独立一行
- `UseChinese`：设置为 `true`

## 命令注册规范

### 1. 注册位置
在 `internal/cli/root.go` 的 `SubCmds` 列表中添加命令：

```go
SubCmds: []qflag.Command{
	PackCmd,
	PreviewCmd,
	UnpackCmd,
	WatchCmd,
	CheckCmd,
	FindCmd,
	HashCmd,
	ListCmd,
	SizeCmd,
	DateCmd,
	EchoCmd,
	RMCmd,
	MkdirCmd,
	TouchCmd,
	<Command>Cmd,  // 新增命令
},
```

### 2. 排序规则
按功能类别排序：
1. 文件操作：Pack, Preview, Unpack
2. 文件检查：Watch, Check
3. 文件查找：Find, Hash, List
4. 文件统计：Size
5. 时间操作：Date
6. 输出操作：Echo
7. 文件管理：RM, Mkdir, Touch
8. 新命令：按功能类别插入合适位置

## 开发流程

### 1. 创建业务逻辑文件
```bash
# 创建命令目录
mkdir internal/commands/<command_name>

# 创建业务逻辑文件
touch internal/commands/<command_name>/cmd_<command_name>.go
```

### 2. 编写业务逻辑
- 定义配置结构体
- 实现主函数
- 实现辅助函数
- 添加错误处理

### 3. 创建 CLI 定义文件
```bash
# 创建 CLI 定义文件
touch internal/cli/<command_name>.go
```

### 4. 编写 CLI 定义
- 定义命令变量
- 定义选项变量
- 实现 init() 函数
- 实现运行函数

### 5. 注册命令
在 `internal/cli/root.go` 中注册命令

### 6. 编译验证
```bash
go build ./internal/cli/...
```

## 代码风格规范

### 1. 注释规范
- 使用函数级注释
- 注释使用中文
- 注释简洁明了

### 2. 错误处理
- 使用 `fmt.Errorf` 包装错误
- 提供友好的错误信息
- 使用 `%w` 包装底层错误

### 3. 变量命名
- 导出变量：首字母大写（如 `MkdirCmd`）
- 私有变量：首字母小写（如 `mkdirParents`）
- 常量：全大写（如 `MAX_DEPTH`）

### 4. 函数命名
- 导出函数：首字母大写（如 `MkdirCmdMain`）
- 私有函数：首字母小写（如 `createDirectory`）

## 完整示例

参考现有命令实现：
- `internal/commands/mkdir/cmd_mkdir.go` - 目录创建命令
- `internal/commands/touch/cmd_touch.go` - 文件创建命令
- `internal/commands/rm/cmd_rm.go` - 文件删除命令
- `internal/cli/mkdir.go` - mkdir CLI 定义
- `internal/cli/touch.go` - touch CLI 定义
- `internal/cli/rm.go` - rm CLI 定义

## 注意事项

1. **包导入**：使用绝对路径导入，如 `"gitee.com/MM-Q/fck/internal/commands/<command_name>"`
2. **错误处理**：所有可能的错误都要处理，提供友好的错误信息
3. **参数验证**：验证所有输入参数的有效性
4. **跨平台**：确保代码在 Windows、Linux、macOS 上都能运行
5. **性能考虑**：处理大量文件时注意性能优化
6. **测试验证**：开发完成后进行编译验证和功能测试

## 快速检查清单

在提交新命令前，检查以下项目：

- [ ] 目录结构符合规范
- [ ] 命名符合规范
- [ ] 业务逻辑文件已创建
- [ ] CLI 定义文件已创建
- [ ] 命令已注册到 root.go
- [ ] 编译通过
- [ ] 帮助文档完整
- [ ] 错误处理完善
- [ ] 代码风格一致
- [ ] 功能测试通过
